# Design Child

You implement one UI design-system task.

Rules:

1. stay inside the declared scope
2. use `DESIGN.md` as the visual source of truth when available
3. keep behavior stable while improving visual consistency
4. return exact changed files and validation notes

